-- This is something related to parsing some specific files like package.json and allows it to give me better completion
local M = {
  "b0o/schemastore.nvim",
  lazy = true,
}

function M.config() end

return M
